/* EasyMarket seller quantity/price hotfix + buyer stock quantity selector v20260918. */
(function(){
'use strict';
function form(){return document.getElementById('sellerProductForm')||document.querySelector('#sellerPanel form');}
function options(){return document.getElementById('sellerPriceOptionsRows');}
function row(qty,price){var box=options();if(!box)return;var r=document.createElement('div');r.style.cssText='display:grid;grid-template-columns:1fr 1fr auto;gap:8px;margin-bottom:8px;align-items:center';r.innerHTML='<input class="seller-input seller-option-qty" type="number" min="1" step="1" placeholder="Количество, шт." value="'+(qty==null?'':qty)+'"><input class="seller-input seller-option-price" type="number" min="0" step="1" placeholder="Цена, ₽" value="'+(price==null?'':price)+'"><button type="button" class="admin-btn danger seller-option-remove" style="padding:9px 12px">×</button>';box.appendChild(r);r.querySelectorAll('input').forEach(function(x){x.addEventListener('input',sync)});r.querySelector('.seller-option-remove').addEventListener('click',function(e){e.preventDefault();r.remove();sync()});sync();}
function sync(){var box=options(),out=[];if(!box)return;box.querySelectorAll(':scope > div').forEach(function(r){var q=Number(r.querySelector('.seller-option-qty')?.value),p=Number(r.querySelector('.seller-option-price')?.value);if(q>0&&p>=0)out.push({qty:Math.floor(q),price:Math.floor(p)})});var seen={};out=out.filter(function(x){if(seen[x.qty])return false;seen[x.qty]=1;return true});var h=document.getElementById('sellerPriceOptions');if(h)h.value=JSON.stringify(out);}
function ensureSeller(){var f=form();if(!f)return;var box=options();if(!box){var wrap=document.createElement('div');wrap.className='form-group seller-price-options-wrap';wrap.innerHTML='<label style="display:block;font-size:13px;font-weight:800;margin-bottom:7px;color:#e7e9ef">💰 Количество и цена</label><div id="sellerPriceOptionsRows"></div><button type="button" id="sellerAddPriceOptionBtn" class="admin-btn secondary" style="width:100%;margin-top:8px">＋ Добавить вариант количества</button><input id="sellerPriceOptions" type="hidden"><div style="font-size:11px;color:#858b99;margin-top:7px">Например: 1 шт. — 100 ₽, 5 шт. — 450 ₽.</div>';var stock=document.getElementById('sellerProductStock');if(stock&&stock.parentNode)stock.parentNode.insertAdjacentElement('afterend',wrap);else f.appendChild(wrap);box=options();}var add=document.getElementById('sellerAddPriceOptionBtn');if(add&&!add.dataset.bound){add.dataset.bound='1';add.addEventListener('click',function(e){e.preventDefault();e.stopPropagation();row();});}if(box&&!box.children.length)row(Number(document.getElementById('sellerProductStock')?.value||1),Number(document.getElementById('sellerProductPrice')?.value||0));sync();}

/* Buyer selector: quantity 1..stock and calculated total. */
(function(){var patched=false;function esc(v){return typeof window.escapeHtml==='function'?window.escapeHtml(String(v??'')):String(v??'').replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]});}function patch(){if(patched||typeof window.openProductModal!=='function')return;patched=true;window.openProductModal=function(id,selectedQty){var p=(window.products||[]).find(function(x){return Number(x.id)===Number(id)}),modal=document.getElementById('productModal'),content=document.getElementById('productModalContent');if(!p||!modal||!content){window.showToast?.('Товар не найден');return}var max=Math.max(1,Math.floor(Number(p.stock)||1)),unit=Math.max(0,Number(p.price)||0),qty=Math.min(max,Math.max(1,Math.floor(Number(selectedQty)||1)));var media=p.image_url?'<img class="product-modal-image" src="'+esc(p.image_url)+'" alt="'+esc(p.name||'')+'">':'<div class="product-modal-icon">'+esc(p.icon||'📦')+'</div>';content.innerHTML=media+'<div class="product-modal-title">'+esc(p.name||'')+'</div><div class="product-modal-label">Количество</div><div style="display:flex;align-items:center;gap:12px;margin:10px 0 14px"><button type="button" id="emQtyMinus" class="buy-btn" style="font-size:22px;min-width:44px">−</button><strong id="emQtyValue" style="font-size:22px;min-width:50px;text-align:center">'+qty+'</strong><button type="button" id="emQtyPlus" class="buy-btn" style="font-size:22px;min-width:44px">＋</button><span style="color:#858b99">из '+max+' шт.</span></div><div class="product-modal-price" id="emTotalPrice">'+unit*qty+' ₽</div>'+(p.description?'<div class="product-modal-label">Описание</div><div class="product-modal-text">'+esc(String(p.description).replace(/<!--EM_PRICES:[\s\S]*?-->/g,'').trim())+'</div>':'')+(p.details?'<div class="product-modal-label">Детали</div><div class="product-modal-text">'+esc(String(p.details).replace(/<!--EM_PRICES:[\s\S]*?-->/g,'').trim())+'</div>':'')+'<div class="product-modal-actions"><button type="button" id="emAddQty" class="buy-btn product-buy-now">⚡ Оформить · '+unit*qty+' ₽</button></div>';function refresh(){document.getElementById('emQtyValue').textContent=qty;document.getElementById('emTotalPrice').textContent=unit*qty+' ₽';document.getElementById('emAddQty').textContent='⚡ Оформить · '+unit*qty+' ₽';document.getElementById('emQtyMinus').disabled=qty<=1;document.getElementById('emQtyPlus').disabled=qty>=max;}document.getElementById('emQtyMinus').onclick=function(){if(qty>1){qty--;refresh()}};document.getElementById('emQtyPlus').onclick=function(){if(qty<max){qty++;refresh()}};document.getElementById('emAddQty').onclick=function(){window.addToCartQuantity?.(Number(p.id),qty)};refresh();modal.classList.add('open');document.body.style.overflow='hidden';};window.addToCartQuantity=function(id,qty){var p=(window.products||[]).find(function(x){return Number(x.id)===Number(id)});if(!p)return false;var max=Math.max(1,Math.floor(Number(p.stock)||1));qty=Math.min(max,Math.max(1,Math.floor(Number(qty)||1)));var unit=Math.max(0,Number(p.price)||0);if((window.cart||[]).some(function(x){return Number(x.id)===Number(id)})){window.showToast?.('Товар уже в корзине 🛒');return true}window.cart.push(Object.assign({},p,{quantity:qty,price:unit*qty,unit_price:unit}));window.saveCart?.();window.updateCart?.();window.showToast?.('Товар добавлен 🛒');window.closeProductModal?.();return true;};}patch();new MutationObserver(patch).observe(document.documentElement,{childList:true,subtree:true});setInterval(patch,500);})();
function init(){ensureSeller();}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();new MutationObserver(init).observe(document.documentElement,{childList:true,subtree:true});setInterval(init,1000);
})();
