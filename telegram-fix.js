/* EasyMarket legacy interaction patch disabled.
   Interaction handling now lives in buyer-options-fix.js to avoid competing
   document-level click handlers in Telegram WebView. */
(function(){ 'use strict'; })();
